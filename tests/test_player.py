from manim_slides.present.player import Player

from typing import Any


#@pytest.fixture()
#def kwargs() -> dict[str, Any]:
#    yield dict(skip_all=True, playback_rate=10.0)
#    yield ("--folder", str(slides_folder), "--skip-all", "--playback-rate", "10")
#
#
#def test_player(kwargs: dict[str, Any]) -> None:
#    runner = CliRunner()
